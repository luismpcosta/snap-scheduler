##
## snap scheduler custom properties
##
snap:
  scheduler:
    enabled: true
    db-polling-interval: 10m
    
spring:

  #
  # Datasource configuration
  #
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:snap;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
